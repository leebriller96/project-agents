# tools/build_report.py 실행에 필요한 파이썬 패키지
# 필수: HTML 레포트 생성
markdown>=3.5
# 권장: 레포트 코드 블록 구문 강조 (없어도 빌드는 됨)
pygments>=2.17

# 선택: PDF 자동 생성을 원할 때만. (보통은 HTML을 브라우저에서 Ctrl+P로 저장하는 것을 권장)
# weasyprint 는 윈도우에서 GTK 런타임 추가 설치가 필요하므로 기본 의존성에서 제외함.
# weasyprint>=60.0
